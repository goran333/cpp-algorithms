bool isSubstring(const char *parent, const char *substring)
{
}

bool isRotation(const char *s1, const char *s2)
{
  //if length is not same, return false
  
  //sort and compare strings, they should be equal, if not return false

  //if s2 is a rotation of s1, then s2 will always be a substring of "s1s1"
}
